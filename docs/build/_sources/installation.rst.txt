Installation
============

1. Clone the repository.
2. Run `pip install kivy mysql-connector-python python-dotenv sphinx`.
3. Create the database using `db_creation.sql`.
4. Set up the `.env` file with DB credentials.
5. Run the app using `python main.py`.
